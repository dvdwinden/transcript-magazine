<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} · TRANSCRIPT Magazine{% else %}TRANSCRIPT Magazine{% endif %}</title>
    <meta name="description" content="{{ description or 'TRANSCRIPT Magazine' }}">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="font-serif">
    <main class="container prose" role="main">
        {{ content | safe }}
    </main>
    <div class="lightbox" id="lightbox">
        <button class="lightbox-close" aria-label="Close lightbox">&times;</button>
        <div class="lightbox-inner"></div>
    </div>
    <script>
        const lightbox = document.getElementById('lightbox');
        const inner = lightbox.querySelector('.lightbox-inner');
        const closeBtn = lightbox.querySelector('.lightbox-close');
        const clickables = document.querySelectorAll('.photo-placeholder, img');

        function closeLightbox() {
            lightbox.classList.remove('active');
            inner.innerHTML = '';
        }

        clickables.forEach(el => {
            if (!lightbox.contains(el)) {
                el.style.cursor = 'pointer';
                el.addEventListener('click', (e) => {
                    e.stopPropagation();
                    inner.innerHTML = '';
                    const clone = el.cloneNode(true);
                    // Ensure reasonable size inside lightbox
                    clone.style.width = '';
                    clone.style.height = '';
                    inner.appendChild(clone);
                    lightbox.classList.add('active');
                });
            }
        });

        lightbox.addEventListener('click', (e) => {
            if (e.target === lightbox) {
                closeLightbox();
            }
        });

        closeBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            closeLightbox();
        });

        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && lightbox.classList.contains('active')) {
                closeLightbox();
            }
        });
    </script>
</body>
</html>
